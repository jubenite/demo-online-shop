server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        native:
          searchLocations: file:///D:/data/repo_local/microservices/demo-evt-sourcing/config-repo